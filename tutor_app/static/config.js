// Configuration constants and DOM element references

// Use relative URL for API calls (works with any domain/port)
var API_BASE_URL = '';

// Cache DOM elements we'll frequently access
var appContainer = document.getElementById('app-container');
var authStatusElement = document.getElementById('auth-status');
